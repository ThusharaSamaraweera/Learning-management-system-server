export enum USER_ROLES {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum USER_STATUS {
  ACTIVE = "ACTIVE",
  ACHIEVED = "ACHIEVED",
}

export enum USER_TITLE {
  STUDENT = "STUDENT",
  LECTURER = "LECTURER",
}